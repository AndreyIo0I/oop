#include <iostream>
#include <string>
#include <map>

using namespace std;

void CountWordOccurrences(std::istream& input, std::ostream& output)
{
	string line;
	while (getline(input, line))
	{

	}

}
